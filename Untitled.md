1、对于解析工具的状态，添加一个 i ，展示详细状态说明

2、添加解析打分，取消默认打分，仅在点击后调用打分

3、批次解析历史，打分历史

4、LLM部分新增VLM管理，允许后续一些VLM评测的接入







review项目代码，识别以下问题：

1、后端报错时前端是否有对应处理，能否展示用户友好的报错信息，信息界面是否合理

2、



开始向量化任务的时候应该可以读取已有的embed model配置，选取嵌入模型和使用的vectorDB，同时如果vectorDB支持一些其他的嵌入特性，也应该做成选项供用户测试选择。这些策略信息应该被记录





很好，目前功能可以走通，但是有体验优化的部分

1、节点配置中，query Expansion这类可以选择已有LLM的，应该让用户去选择一个已有的LLM/agent来使用，目前无法选择（这个逻辑很奇怪，我没有配置LLM却可以使用HyDE之类的策略）

2、reranker、fliter节点选择的时候，在下拉框中应该可以同步看到模块可用状态，否则我无法知道哪个模块可用，下拉框应该把可用组件排序在最前面

3、节点配置现在使用json展示，对用户不友好，也没有json格式校验，优化这个体验。我看到默认的max_output_tokens是256，为什么这样设置？



现在开始检索策略功能开发

参考AutoRAG项目，接入BM25、VectorDB、Hybrid RRF、HybridCC

将向量数据库也作为一个节点在“流程构建”中体现

默认选取策略VectorDB，但是也可以选择其他策略

并且允许用户手动调整混合检索情况下的权重分配





















